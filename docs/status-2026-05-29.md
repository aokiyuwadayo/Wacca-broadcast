# YUWA Broadcast 現状スナップショット

> **スナップショット日**: 2026-05-29
> **目的**: 別PCでクローンした人（or Claude Code セッション）が「今ここ」を正確に把握し、続行できるようにする
> **更新方針**: 大きな進展があったら新しい `status-YYYY-MM-DD.md` を追加。古いものは履歴として残す

---

## TL;DR（3 行）

1. **設計確定＆実地検証済**。`docs/design-doc.md` に全体設計、本ファイルに現在地。
2. **Phase A（核：メモ→各PF文面→コピペ）実装済 → PR #1**。ビルド・型チェック・起動・画面・エラー処理まで実機グリーン。
3. **残るは「実 AI 生成 1 回」のローカル確認だけ** → OK なら PR #1 マージ → Phase B（Supabase+Vercel化）へ。

---

## これまでの流れ

ヒアリング（痛みの特定）→ 設計（論点 A〜F）→ コードを書く前に Claude を「Phase A エンジン」に見立てた
**実地検証**（6/4 立花祭の出展内容決め回 / design-doc §14）→ 設計ドキュメント化 → Phase A 実装（PR #1）。

### 特定した痛み
毎週の活動・イベント情報を LINE グループ / Teams に共有する作業が重い：①投稿先が多い ②見やすい文章を
作るのが面倒 ③先延ばし・投稿忘れ。1 回 30 分〜1 時間 × 毎週。

---

## ✅ 完了

- **設計ドキュメント** `docs/design-doc.md`（v0.1）：コンセプト / 中間JSON スキーマ / PF 書き分け /
  オンボーディング方針 / 神機能 / 権限・引き継ぎ / 失敗時挙動 / 技術構成・コスト / ロードマップ /
  実地検証ログ / 名前候補 / 未決事項。
- **実地検証（6/4）**：中間JSON → LINE版/Teams版 の生成が機能することを実データで確認。得られた文面は
  そのまま今週コピペで使える質だった（青木が「素晴らしい」と評価）。
- **Phase A 実装（PR #1, ブランチ `feat/phase-a-generator`）**：
  - 種別（活動/イベント）選択、ガイド型（不足項目を質問）/ フォーム型（項目直接編集）、
    各PF（LINE/Teams/Discord）書き分け生成＋コピペ、AI相談で修正・再生成、AIの推測(assumptions)可視化。
  - Next.js 15 + Tailwind v3 + Anthropic SDK（tool use 強制 + prompt caching）。既定 Haiku 4.5。
  - 検証：`npm install` / `npm run build`（型・Lint）/ 起動 / 画面描画 / キー無し時のエラー処理 まで実機グリーン。
    **実 AI 生成のみ未確認**（API キーが要るため。質は 6/4 検証で確認済み）。
- **CLAUDE.md**：別PCでも文脈を掴めるプロジェクトガイド。

## ⬜ 次にやること

1. **青木がローカルで PR #1 を実機確認**（業務PC以外で）：
   ```bash
   git clone https://github.com/aokiyuwadayo/yuwa-broadcast.git
   cd yuwa-broadcast && git checkout feat/phase-a-generator
   npm install
   echo 'ANTHROPIC_API_KEY=sk-...' > .env.local
   npm run dev   # http://localhost:3000
   ```
   6/4 のメモを入れて手動検証と同じ質の文面が出るか確認 → OK なら **PR #1 をマージ**。
2. **Phase B**：Supabase + Vercel 化（スマホ対応 PWA・保存・履歴・前回複製）。
3. 以降 design-doc §13 の Phase C（接続ウィザード＋自動投稿）→ D（予約・定期下書き・Slack リマインド）→
   E（文体学習・複数管理者）→ F（スライド下書き生成）。

## 🟡 決定済みで覚えておくこと

- **AI/コスト方針**：中央キー 1 個＋Haiku。学生（エンドユーザー）はキー不要。悪用はログイン＋回数制限で防ぐ。
  3（無料枠の別LLM）/ 4（無料テンプレ＋任意AIのハイブリッド）は「多サークル＝商用化」が見えてから検討。
- **セキュリティ**：API キーはローカルの `.env.local` のみ（`.gitignore` 済、コミット不可を確認済）。
  会話やトランスクリプトに生キーを出さない。

## ⚠ 未決事項（運営メンバーと相談）

- **名前**：本名「青木優和（ゆうわ）」の要素を活かす方向。候補 Musubi / YUWA Cast / 結伝 / Wacca。当面の仮称 **YUWA Broadcast**。
- 活動テンプレ 8 項目が運用で長すぎないか。
- 他サークル開放時の運用・商用化（Vercel Pro 費用）の扱い。

## 📎 関連

- 設計: [`docs/design-doc.md`](design-doc.md)
- プロジェクトガイド: [`../CLAUDE.md`](../CLAUDE.md)
- Phase A PR: https://github.com/aokiyuwadayo/yuwa-broadcast/pull/1
- 姉妹リポ（YUWA 本体, 旧 s-fit）: https://github.com/aokiyuwadayo/yuwa
