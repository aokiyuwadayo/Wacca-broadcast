# Wacca Cast 現状スナップショット

> **スナップショット日**: 2026-06-10
> **目的**: 別PC/別セッションが「今ここ」を掴んで続行できるようにする
> **前回**: status-2026-06-08.md（Issue #8 のコードは「個人PCで実装」とあるが、実際は本日全て main 入り）

---

## TL;DR（3行）
1. **Issue #8（複数管理者・引き継ぎ）のコード実装は PR #28/#29/#30 で完了**。認証 scaffold（#19）→ account_id 紐付け → RLS マイグレーション → メンバー招待/owner 移譲 UI まで main 入り。
2. **Slack が4つ目の配信PF**（#27）。S-FIT 導入ランブックは `docs/sfit-slack-setup.md`。
3. **残りは全てダッシュボード/ブラウザ作業**: Slack Webhook 2本、Supabase マイグレーション実行（slack/0001/backfill/0002 の順）、Auth 有効化、B系プロンプトの実出力検証。

## 本日 main に入ったもの（2026-06-10）
- **#25**: Supabase 未設定でも /settings が壊れない（GET 空フォールバック + save() の silent fail 修正）
- **#26**: /api/schedule の未設定ガード + 3ルートの route handler 単体テスト + vitest `@` alias
- **#27**: Slack 告知配信PF（schema/prompts/UI/post/cron + `migration_add_slack_announce.sql`）
- **#28**: `getAccountId()` / `accountFields()` — ログイン中だけ account_id で絞り込み/付与（未ログインは従来挙動 = 非破壊）
- **#29**: `supabase/migrations/0002_enable_rls.sql` — RLS 有効化（`my_account_ids()` security definer で再帰回避。**実行はまだ**）
- **#30**: `/api/members`（GET/POST/DELETE）+ `/api/members/transfer` + 設定画面の 👥 メンバーセクション（ログイン時のみ表示）

テスト: vitest 44 passed / tsc クリーン / build 成功（各PRで確認済み）

## 残作業（全てブラウザ/ダッシュボード。会社PCの個人ログインは 2026-06-10 に青木が解禁済み）
1. **S-FIT Slack 導入**（`docs/sfit-slack-setup.md`）: Webhook 2本（#00_announce / #100_notifications）→ `migration_add_slack_announce.sql` 実行 → 設定画面で登録+テスト送信
2. **認証ロールアウト**（`docs/phase-e-auth-plan.md` の順番厳守）:
   a. `0001_accounts_memberships.sql` 実行
   b. Supabase Auth 有効化（Magic Link が最小。青木のメールでログイン確認）
   c. accounts/memberships 作成 + 既存行 backfill（0001 末尾コメント）
   d. `0002_enable_rls.sql` 実行 → ファイル末尾のチェックリストで確認
   e. Vercel に `REQUIRE_AUTH=1`（全部通ってから）
3. **B系プロンプト実出力検証**（`docs/prompt-improvements-proposal.md` のチェックリスト。B1/B2 適用済み、判定だけ未）

## ガードレール（更新）
- 個人アカウントのブラウザログインは**この会社PCでも OK**（2026-06-10 青木の明示指示）。
- 実キーをローカルファイルに置く・大きい課金操作は引き続き事前確認。
- 個人リポなので build/型/テストが通ればマージ自走可。

## 関連
- [設計](design-doc.md) / [認証プラン](phase-e-auth-plan.md) / [Slack導入](sfit-slack-setup.md) / [プロンプト改善](prompt-improvements-proposal.md) / [デプロイ](deploy.md)
